// api/login end point
const router = require('express').Router();
const { comment } =require('../../model/Comment')

//require proper model (login)


//get route to grab all login info?
router.get('/' , (req, res) => {} );

module.exports = router;