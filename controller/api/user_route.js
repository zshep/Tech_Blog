const router = require('express').Router();
const { User, Post, Comment } = require('../../model');


//require proper model (message)

// a get route for something...
router.get('/', async (req, res) => {


});

module.exports = router;
